# Domain name under which the y-websocket server will be reachable.
# Must be a publicly resolvable DNS name pointing to this host
# so that Caddy can obtain a Let's Encrypt certificate via HTTP-01 challenge.
DOMAIN=your-domain.example.com
